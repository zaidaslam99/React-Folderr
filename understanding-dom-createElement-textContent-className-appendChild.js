// Working with createElement()
const header = document.createElement("h1");

// Setting the text content of the header
header.textContent = "Hello, Lebron is here!";

// Adding a classname for the header
header.className = "header";

// Appending the header to the body
document.body.appendChild(header);
